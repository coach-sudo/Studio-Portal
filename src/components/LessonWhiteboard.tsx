import { Eraser, Highlighter, MousePointer2, Pencil, Redo2, Save, Table2, TextCursorInput, Undo2, Upload } from "lucide-react";
import { useMemo, useRef, useState, type PointerEvent as ReactPointerEvent } from "react";
import { useQueryClient } from "@tanstack/react-query";
import type { Lesson, LessonWhiteboard, Material, Student, StudioSnapshot, WhiteboardElement } from "../domain/model";
import { studioCommand } from "../data/bookingCommands";
import { uploadStudioFile } from "../data/uploads";

type Tool="select"|"draw"|"highlight"|"erase"|"text"|"table";
const freshDocument=()=>({version:1,elements:[] as WhiteboardElement[]});

export function LessonWhiteboardBoard({data,lesson,student,isDemo,onDemoChange}:{data:StudioSnapshot;lesson:Lesson;student:Student;isDemo:boolean;onDemoChange?:(board:LessonWhiteboard)=>void}){
  const existing=data.lessonWhiteboards.find((item)=>item.lessonId===lesson.id), initial=useMemo(()=>structuredClone(existing?.document||freshDocument()),[existing?.id]);
  const [document,setDocument]=useState(initial),[history,setHistory]=useState<typeof document[]>([]),[future,setFuture]=useState<typeof document[]>([]),[tool,setTool]=useState<Tool>("select"),[color,setColor]=useState("#173f35"),[width,setWidth]=useState(3),[fontSize,setFontSize]=useState(20),[fontFamily,setFontFamily]=useState("Inter"),[underline,setUnderline]=useState(false),[notice,setNotice]=useState(""),[saving,setSaving]=useState(false),[dirty,setDirty]=useState(false),[assetUrls,setAssetUrls]=useState<Record<string,string>>({});
  const board=useRef<HTMLDivElement>(null),drawing=useRef<string|undefined>(undefined),dragging=useRef<{id:string;dx:number;dy:number}|undefined>(undefined),queryClient=useQueryClient();
  const commit=(next:typeof document)=>{setHistory((items)=>[...items.slice(-49),structuredClone(document)]);setFuture([]);setDocument(next);setDirty(true);};
  const point=(event:ReactPointerEvent)=>{const rect=board.current!.getBoundingClientRect();return{x:Math.max(0,Math.min(1200,(event.clientX-rect.left)*1200/rect.width)),y:Math.max(0,Math.min(720,(event.clientY-rect.top)*720/rect.height))};};
  const pointerDown=(event:ReactPointerEvent)=>{const value=point(event);if(tool==="draw"||tool==="highlight"){const id=crypto.randomUUID(),element:WhiteboardElement={id,type:"path",points:[value],color:tool==="highlight"?"#f4d35e":color,width:tool==="highlight"?Math.max(14,width*4):width,highlighted:tool==="highlight"};drawing.current=id;commit({...document,elements:[...document.elements,element]});(event.currentTarget as HTMLElement).setPointerCapture(event.pointerId);}else if(tool==="erase"){const target=[...document.elements].reverse().find((item)=>item.type==="path"?item.points.some((entry)=>Math.hypot(entry.x-value.x,entry.y-value.y)<=Math.max(18,item.width*1.5)):value.x>=item.x&&value.x<=item.x+item.width&&value.y>=item.y&&value.y<=item.y+item.height);if(target)commit({...document,elements:document.elements.filter((entry)=>entry.id!==target.id)});}else if(tool==="text"){const text=window.prompt("Text to add");if(text)commit({...document,elements:[...document.elements,{id:crypto.randomUUID(),type:"text",x:value.x,y:value.y,width:280,height:90,text,color,fontSize,fontFamily,underline}]});}else if(tool==="table"){const size=window.prompt("Table size (rows x columns)","3x3")||"",match=size.match(/^(\d+)\s*x\s*(\d+)$/i);if(match){const rows=Math.min(10,Math.max(1,Number(match[1]))),columns=Math.min(8,Math.max(1,Number(match[2])));commit({...document,elements:[...document.elements,{id:crypto.randomUUID(),type:"table",x:value.x,y:value.y,width:420,height:Math.max(120,rows*44),rows,columns,cells:Array(rows*columns).fill("")}]});}}};
  const pointerMove=(event:ReactPointerEvent)=>{const value=point(event);if(drawing.current)setDocument((current)=>({...current,elements:current.elements.map((item)=>item.id===drawing.current&&item.type==="path"?{...item,points:[...item.points,value]}:item)}));if(dragging.current){const {id,dx,dy}=dragging.current;setDocument((current)=>({...current,elements:current.elements.map((item)=>item.id===id&&item.type!=="path"?{...item,x:value.x-dx,y:value.y-dy}:item)}));setDirty(true);}};
  const pointerUp=()=>{drawing.current=undefined;dragging.current=undefined;};
  const objectPointer=(event:ReactPointerEvent,item:Exclude<WhiteboardElement,{type:"path"}>)=>{event.stopPropagation();if(tool==="erase"){commit({...document,elements:document.elements.filter((entry)=>entry.id!==item.id)});return;}if(tool!=="select")return;const value=point(event);dragging.current={id:item.id,dx:value.x-item.x,dy:value.y-item.y};(event.currentTarget as HTMLElement).setPointerCapture(event.pointerId);};
  const undo=()=>{const previous=history.at(-1);if(!previous)return;setFuture((items)=>[structuredClone(document),...items]);setDocument(previous);setHistory((items)=>items.slice(0,-1));setDirty(true);};
  const redo=()=>{const next=future[0];if(!next)return;setHistory((items)=>[...items,structuredClone(document)]);setDocument(next);setFuture((items)=>items.slice(1));setDirty(true);};
  const save=async()=>{if(saving)return;setSaving(true);try{if(isDemo){onDemoChange?.({id:existing?.id||`whiteboard-${crypto.randomUUID()}`,studioId:data.studioId,lessonId:lesson.id,document,version:(existing?.version||0)+1,updatedAt:new Date().toISOString()});}else{await studioCommand("whiteboards",{command:"save",entityId:lesson.id,expectedVersion:existing?.version||0,payload:{document},reason:"Lesson whiteboard saved"});await queryClient.invalidateQueries({queryKey:["studio"]});}setDirty(false);setNotice("Whiteboard saved for both coach and student.");}catch(reason){setNotice(reason instanceof Error?reason.message:"Whiteboard could not be saved.");}finally{setSaving(false);}};
  const addPdf=async(file?:File)=>{if(!file)return;if(file.type!=="application/pdf"){setNotice("Choose a PDF to place on the board.");return;}setSaving(true);try{if(isDemo){setNotice("PDF placement uses secure file storage in the live studio.");return;}const stored=await uploadStudioFile({studioId:data.studioId,studentId:student.id,entityType:"lesson",entityId:lesson.id,file,visibility:"student"}),result=await studioCommand("materials",{command:"create",expectedVersion:0,payload:{studentId:student.id,lessonId:lesson.id,title:file.name,category:"Script",storagePath:stored.storagePath,mimeType:file.type,fileSizeBytes:file.size,mediaKind:"document",role:"lesson_material"},reason:"PDF added to lesson whiteboard"}),materialId=String(result.resource.id);setAssetUrls((urls)=>({...urls,[materialId]:stored.signedUrl||""}));commit({...document,elements:[...document.elements,{id:crypto.randomUUID(),type:"pdf",x:80,y:80,width:520,height:560,materialId}]});setNotice("PDF rendered on the board. Save when placement is ready.");await queryClient.invalidateQueries({queryKey:["studio"]});}catch(reason){setNotice(reason instanceof Error?reason.message:"PDF could not be added.");}finally{setSaving(false);}};
  const materialUrl=(id:string)=>assetUrls[id]||data.materials.find((item:Material)=>item.id===id)?.externalUrl||"";
  return <section className="whiteboard-workspace" aria-label="Lesson whiteboard"><header><div><h2>Lesson whiteboard</h2><p>Shared visual workspace · {dirty?"unsaved changes":"saved"}</p></div><div className="whiteboard-history"><button type="button" onClick={undo} disabled={!history.length} aria-label="Undo"><Undo2/></button><button type="button" onClick={redo} disabled={!future.length} aria-label="Redo"><Redo2/></button><button type="button" className="primary-button" onClick={()=>void save()} disabled={saving||!dirty}><Save/>{saving?"Saving…":"Save board"}</button></div></header>{notice&&<p className="portal-notice" role="status">{notice}</p>}<div className="whiteboard-toolbar" role="toolbar" aria-label="Whiteboard tools">{([ ["select",MousePointer2,"Move"],["draw",Pencil,"Draw"],["highlight",Highlighter,"Highlight"],["erase",Eraser,"Erase"],["text",TextCursorInput,"Text"],["table",Table2,"Table"]] as const).map(([id,Icon,label])=><button type="button" key={id} className={tool===id?"selected":""} onClick={()=>setTool(id)} title={label}><Icon/><span>{label}</span></button>)}<label>Color<input type="color" value={color} onChange={event=>setColor(event.target.value)}/></label><label>Stroke<input type="range" min="1" max="12" value={width} onChange={event=>setWidth(Number(event.target.value))}/></label><label>Font<select value={fontFamily} onChange={event=>setFontFamily(event.target.value)}><option>Inter</option><option>Georgia</option><option>Courier New</option></select></label><label>Size<input type="number" min="10" max="72" value={fontSize} onChange={event=>setFontSize(Number(event.target.value))}/></label><button type="button" className={underline?"selected":""} onClick={()=>setUnderline(!underline)}><u>U</u><span>Underline</span></button><label className="whiteboard-upload"><Upload/><span>Add PDF</span><input type="file" accept="application/pdf" onChange={event=>void addPdf(event.target.files?.[0])}/></label></div><div ref={board} className={`whiteboard-board tool-${tool}`} onPointerDown={pointerDown} onPointerMove={pointerMove} onPointerUp={pointerUp} onPointerCancel={pointerUp}><svg viewBox="0 0 1200 720" preserveAspectRatio="none">{document.elements.filter((item):item is Extract<WhiteboardElement,{type:"path"}>=>item.type==="path").map((item)=><polyline key={item.id} points={item.points.map((value)=>`${value.x},${value.y}`).join(" ")} fill="none" stroke={item.color} strokeWidth={item.width} strokeLinecap="round" strokeLinejoin="round" opacity={item.highlighted?.45:1}/>)}</svg>{document.elements.filter((item):item is Exclude<WhiteboardElement,{type:"path"}>=>item.type!=="path").map((item)=><div key={item.id} className={`whiteboard-object ${item.type}`} style={{left:`${item.x/12}%`,top:`${item.y/7.2}%`,width:`${item.width/12}%`,height:`${item.height/7.2}%`}} onPointerDown={event=>objectPointer(event,item)}>{item.type==="text"&&<div style={{color:item.color,fontSize:item.fontSize,fontFamily:item.fontFamily,textDecoration:item.underline?"underline":undefined,background:item.highlighted?"#f4d35e66":undefined}}>{item.text}</div>}{item.type==="table"&&<table><tbody>{Array.from({length:item.rows},(_,row)=><tr key={row}>{Array.from({length:item.columns},(_,column)=><td key={column} contentEditable suppressContentEditableWarning onBlur={event=>{const cells=[...item.cells];cells[row*item.columns+column]=event.currentTarget.textContent||"";setDocument((current)=>({...current,elements:current.elements.map((entry)=>entry.id===item.id&&entry.type==="table"?{...entry,cells}:entry)}));setDirty(true);}}>{item.cells[row*item.columns+column]}</td>)}</tr>)}</tbody></table>}{item.type==="pdf"&&(materialUrl(item.materialId)?<object data={materialUrl(item.materialId)} type="application/pdf" aria-label="Whiteboard PDF">PDF preview unavailable. <a href={materialUrl(item.materialId)} target="_blank" rel="noreferrer">Open PDF</a></object>:<div className="pdf-placeholder">PDF is loading…</div>)}</div>)}</div><footer><small>Draw and highlights appear over scripts and PDFs. Move repositions objects; Erase removes the item under the pointer. Changes are shared only after Save.</small></footer></section>;
}
